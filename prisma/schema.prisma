generator client {
  provider = "prisma-client"
  output   = "../prisma_client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    String  @id @default(uuid())
  email String  @unique
  name  String?
}
